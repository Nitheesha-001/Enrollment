projection;
strict ( 2 );
use draft;

define behavior for ZUJ_CONT_C alias Contract
{
  use create;
  use update;
  use delete;

  use action CancelEnrollment;
  use action SuspendEnrollment;
  use action ReactivateEnrollment;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}